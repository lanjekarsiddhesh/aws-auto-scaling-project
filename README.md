# aws-auto-scaling-project
Built a highly available and self-healing web infrastructure on AWS using EC2, ASG, Launch Templates, Application Load Balancer, AMI, EBS, IAM. The architecture automatically scales based on demand, replaces failed instances, and distributes traffic across multiple servers to ensure reliability, fault tolerance, and high availability.
